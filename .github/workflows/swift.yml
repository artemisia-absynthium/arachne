name: Swift

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: swift build -v
    - name: Run tests
      run: |
        echo "::set-output name=CODECOV_PATH::$(swift test --show-codecov-path)"
      id: test
    - uses: codecov/codecov-action@v2
      with:
        files: ${{ steps.test.outputs.CODECOV_PATH }}
