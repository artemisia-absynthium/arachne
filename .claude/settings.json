{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path // empty'); case \"$file\" in *\".claude/rules/synced\"*) echo \"ERROR: .claude/rules/synced/ is sync-managed — edits are overwritten on the next sync. Add rules to .claude/rules/ instead.\" >&2; exit 2;; esac"
          }
        ]
      }
    ]
  }
}
